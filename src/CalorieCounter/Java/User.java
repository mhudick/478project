package CalorieCounter.Java;

/**
 * Created by Mike on 9/26/2015.
 */
public class User {

    //this will be the master class from which all other classes will rely.
    //from a design perspective we want to have a user who has many recipes, and foodItems and whatever else we decide to add
    //we will need to have a class for each table we create.  We will need to check the JSON object returned from the API
    //to see what fields will be in our database for foods.

}
